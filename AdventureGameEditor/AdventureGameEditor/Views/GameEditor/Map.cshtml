@model AdventureGameEditor.Models.MapViewModel
@{
    ViewData["Title"] = "Map";
}

<table border="1" frame="box" rules="all" >
    @for(int i = 0; i < Model.MapSize; i++)
    {
    <tr>
        @for (int j = 0; j < Model.MapSize; j++)
        {
            <td style="height:100px;width:100px">
                <a asp-area="" asp-controller="GameEditor" asp-action="Test"
                   asp-route-gameTitle=@Model.GameTitle asp-route-rowNumber=@i
                   asp-route-colNumber=@j asp-route-targetPicID=@Model.TargetPicID>
                    semmi
                </a>
            </td>
            }
        </tr>
    }
    <tr>
    </tr>
</table>
