@model AdventureGameEditor.Models.LoadMapViewModel
@{
    ViewData["Title"] = "Map";
}


<table>
    @foreach (MapRow row in Model.Map)
    {
        <tr>
            @foreach (Field field in row.Row)
            {
                <td >
                    <button type="button"  onclick=" @Model.FunctionName ('@Model.GameTitle', '@field.RowNumber', '@field.ColNumber');">
                        <div id="@field.RowNumber@field.ColNumber">
                            @{
                                await Html.RenderPartialAsync("FieldPartialView",
                                new MapPieceViewModel
                                {
                                    GameTitle = Model.GameTitle,
                                    Field = field
                                });
                            }
                         </div>
                    </button>
                </td>
            }
        </tr>
    }
</table>

<script type="text/javascript" src="@Url.Content("/js/site.js")"></script>





